spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

  # ============================================================
  # BASE DE DATOS — SUPABASE (produccion)
  # ============================================================
  datasource:
    url: jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:6543/postgres?user=postgres.wfgbepodmukcfupozmaa&password=KWiif9Ah_j/Tth5
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: localhost
    port: 25
    properties:
      mail:
        smtp:
          auth: false

# ============================================================
# CONFIGURACION DE LA APLICACION
# ============================================================
app:
  jwt:
    secret: ${JWT_SECRET:ClaveDesarrolloLocalNoUsarEnProduccion}
    expiration-ms: 86400000
    refresh-expiration-ms: 604800000

  verificacion:
    minutos-expiracion: 15
    longitud-codigo: 6

  frontend-url: ${FRONTEND_URL:http://localhost:3000}

  cors:
    origenes-permitidos: ${CORS_ORIGINS:http://localhost:3000,http://localhost:5173}

  n8n:
    webhook-verificacion: ${N8N_WEBHOOK_VERIFICACION:http://localhost:5678/webhook-test/verificacion-correo}
    webhook-bienvenida: ${N8N_WEBHOOK_BIENVENIDA:http://localhost:5678/webhook-test/bienvenida-correo}

server:
  port: 8080
  address: 0.0.0.0

logging:
  level:
    com.finanzas: DEBUG
    org.springframework.security: INFO



